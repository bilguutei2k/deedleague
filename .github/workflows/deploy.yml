name: Deploy (inactive)

on:
  workflow_dispatch:

jobs:
  inactive:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    permissions:
      contents: read
    steps:
      - name: Explain why deployment is inactive
        run: |
          echo "::error title=Inactive deployment::No Vercel project ownership, project IDs, or deployment token are configured."
          exit 1
